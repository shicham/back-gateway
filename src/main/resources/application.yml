spring:
  application:
    name: api-back-gateway

server:
  port: 8000

eureka:
  instance:
    leaseRenewalIntervalInSeconds: 5
  client:
    serviceUrl:
      defaultZone: http://51.68.44.179:8700/eureka/

# Api-Gate-Way
zuul:
  ignoredServices: "*"
  prefix: /api
  routes:
    api-back-organization:
      path: /api-back-organization/**
      #serviceId: API-BACK-ORGANIZATION
      url: http://51.68.44.179:8010/api-back-organization
      sensitiveHeaders: Cookie,Set-Cookie
  host:
    connect-timeout-millis: 10000
    socket-timeout-millis: 1000000

hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 1000000
